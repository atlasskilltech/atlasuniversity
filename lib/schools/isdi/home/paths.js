/**
 * Local roots for the ISDI homepage's staged assets.
 *
 * Photographs heavier than 150KB were re-encoded as WebP q95 (SSIM >= 0.985 at
 * full resolution); everything else is byte-for-byte. Videos keep only the
 * source a browser plays — the .mp4, or the .webm where the reference's .mp4 is
 * broken upstream.
 */
export const IMG = '/assets/images/schools/isdi/home';
export const VID = '/assets/videos/schools/isdi/home';
